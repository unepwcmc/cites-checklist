Filter by country:
{{view Checklist.CountrySelect}}
<p>Selected: {{Checklist.SelectedCountryController.country.name}}</p>

<ul>
{{#each taxonConcepts}}
  <li>
    <p class="level{{unbound depth}}">
      {{full_name}}
    </p>
  </li>
{{/each}}
</ul>
